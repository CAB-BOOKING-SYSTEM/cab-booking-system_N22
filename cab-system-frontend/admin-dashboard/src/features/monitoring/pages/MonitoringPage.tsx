import { PageIntro } from "../../../shared/ui/PageIntro";

export function MonitoringPage() {
  return (
    <PageIntro
      title="Monitoring"
      description="Giám sát Hệ thống: Bao gồm giám sát bản đồ thời gian thực (real-time map), logs và kiểm toán (audit)."
    />
  );
}
